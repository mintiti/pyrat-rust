[package]
name = "pyrat-engine-interface"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pyrat = { package = "pyrat-rust", path = "../engine", default-features = false }

[lints]
workspace = true
