[package]
name = "pyrat-protocol"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pyrat-rust = { path = "../../engine", default-features = false }
pyrat-wire = { path = "../wire" }

[lints]
workspace = true
