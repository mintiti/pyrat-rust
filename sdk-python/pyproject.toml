[project]
name = "pyrat-sdk"
version = "0.1.0"
description = "Python SDK for building PyRat bots"
requires-python = ">=3.10"
dependencies = [
    "flatbuffers>=24.0.0",
    "numpy>=1.20.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
